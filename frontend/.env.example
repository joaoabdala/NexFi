# URL base da API para o desenvolvimento local (backend rodando com uvicorn na porta 8000).
# Em produção não é preciso configurar nada: o build usa .env.production (VITE_API_URL=/api/v1),
# já que frontend e API são servidos no mesmo domínio.
VITE_API_URL=http://localhost:8000/api/v1
