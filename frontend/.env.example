# URL base da API. Local: backend rodando com uvicorn na porta 8000.
# Na Vercel, defina esta variável no projeto do frontend com a URL do projeto do backend,
# ex.: https://nexfi-api.vercel.app/api/v1 (é embutida no build — redeploy após alterar).
VITE_API_URL=http://localhost:8000/api/v1
